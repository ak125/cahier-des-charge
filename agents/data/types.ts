/**
 * Types spécifiques aux agents de la catégorie data
 * Ce fichier étend les types de base pour les spécialiser
 */

import { AgentOptions, AgentResult } from '../core/types';

/**
 * Options spécifiques aux agents data
 */
export type DataAgentOptions = AgentOptions;

/**
 * Résultat spécifique aux agents data
 */
export type DataResult = AgentResult;
