import React from reactstructure-agent';
import ReactDOM from react-dom/clientstructure-agent';
import Dashboard from ./components/Dashboardstructure-agent';
import ./styles.cssstructure-agent';

const root = ReactDOM.createRoot(
  document.getElementById('root') as HTMLElement
);

root.render(
  <React.StrictMode>
    <Dashboard />
  </React.StrictMode>
);