/**
 * Types spécifiques aux agents de la catégorie utils
 * Ce fichier étend les types de base pour les spécialiser
 */

import { AgentOptions, AgentResult } from '../core/types';

/**
 * Options spécifiques aux agents utils
 */
export type UtilsAgentOptions = AgentOptions;

/**
 * Résultat spécifique aux agents utils
 */
export type UtilsResult = AgentResult;
